import type { BattleMode } from '@/types'

export const BATTLE_MODE_LABELS: Record<BattleMode, string> = {
  dungeon: '🏰 Dungeon Crawl',
  chaos_tower: '🗼 Chaos Tower',
  guild_raid: '⚔ Guild Raid',
  event: '🌟 Event Battle',
}
