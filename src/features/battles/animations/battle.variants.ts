import type { Variants } from 'framer-motion'
import { fadeInUp, scaleIn } from '@/lib/animations'

export const battlePanelEnter: Variants = fadeInUp
export const battleResultPop: Variants = scaleIn
